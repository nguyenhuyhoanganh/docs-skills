# Diagram Docs Workflow

Use skill `doc-diagrams-as-code`.

Then:

1. Select diagram type based on document section (context/container/component/sequence/deployment/dataflow).
2. Author diagrams in PlantUML by default.
3. Use Mermaid only with explicit fallback reason.
4. Attach source and rendered artifacts to target Confluence page.
