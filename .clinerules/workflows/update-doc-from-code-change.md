# Update Doc From Code Change Workflow

Use skills `doc-context-discovery` and `doc-lifecycle-management`.

Then:

1. Identify changed modules/contracts from code diff.
2. Locate impacted Confluence pages by keyword and ownership.
3. Apply delta updates with version bump and change summary.
4. Re-run quality gates if contract or architecture changed.
