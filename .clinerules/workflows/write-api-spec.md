# Write API Spec Workflow

Use skill `doc-api-spec-authoring`.

Then:

1. Define API/event contracts with compatibility policy.
2. Specify error model, idempotency, and versioning strategy.
3. Link every endpoint/event back to requirement IDs.
