# Update Existing Docs Workflow

Use skills `doc-context-discovery` and `doc-lifecycle-management`.

Then:

1. Discover related pages via keyword and semantic search.
2. Decide update-in-place, versioned update, or deprecate/archive.
3. Apply lifecycle policy with metadata and ownership updates.
