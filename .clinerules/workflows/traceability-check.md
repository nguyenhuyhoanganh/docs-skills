# Traceability Check Workflow

Use skill `doc-traceability-and-gates`.

Then:

1. Use this workflow when traceability/governance artifact is requested or required.
2. Build requirement-to-artifact traceability matrix for in-scope artifacts.
3. Check quality gates and unresolved risk items.
4. Block publish until all required gates pass.
