# Publish Confluence Workflow

Use skill `doc-confluence-publishing`.

Then:

1. Upsert pages with metadata and governance tags.
2. Create review tasks and publish status transitions.
3. Record version and change summary in parent index page.
